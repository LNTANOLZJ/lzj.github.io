# 针对「软件工程师（上位机软件）」的简历优化说明

招聘方：海生精密（东莞）　薪资：8000–13000　关键词：C++ / Qt / STL　年限：1–3 年

## 岗位要求 → 简历对应

| 招聘要求 | 原简历问题 | 本次调整 |
| --- | --- | --- |
| 精通 C/C++，熟悉 Qt、STL，熟练 Visual Studio，有 Windows 界面经验 | C++ 特性写偏学院（可变参数模板等），Windows 界面不够醒目 | 按岗位顺序重排：C/C++、STL、Qt Widgets、Visual Studio；界面写到控件级（QTreeView / Model/View） |
| 规范开发流程、编程风格、文档能力 | 有，但散落在末条 | 技能与工作经历都单独写出文档 / 模块化 / 接口说明 |
| **了解串口/网络，懂 PLC，懂 OPC；PLC 通信经验优先** | 已有 OPC UA + PLC，但是第 2 条，容易被视觉/OSG 淹没 | **提前并加粗** OPC UA、PLC 读写/订阅/断线重连；网络通信用 OPC UA（TCP）承接；串口仅写「了解」，未编造项目 |
| 自动化兴趣、细致、沟通、融入团队 | 原件无自我评价 | 补自我评价，全部落到上位机联调，避免空话 |
| 职位名：上位机软件 | 未写求职意向 | 页眉直接写「软件工程师（上位机软件）」 |

## 结构问题

原「工作经历」和「项目经历」几乎同一段话复制了两遍。现改为：

- **工作经历**：对公司负责什么、交付什么（岗位职责视角）
- **项目经历**：同一套上位机，写成技术闭环（工艺编辑 → PLC → 视觉 → 3D），突出实现细节

## 未改、请你本人补上的内容

1. 姓名、电话、邮箱、教育背景（原截图未包含）。
2. 工作时长目前是 2026.04–2026.09（约半年）。岗位写的是 2–3 年，**不要把年限写满**，用项目匹配度说话。
3. 若实际做过 **RS232 / Modbus / TCP 自定义协议**，请补一条真实协议名，比写「了解串口」更有用。
4. 视觉定位若有可量化结果（节拍、对准成功率），可替换定性「提高精度」。
5. 原错别字已改：`lamba` → lambda，`右值应用` → 右值引用（现稿已弱化学术特性列表）。
6. **3D / OSG 已按事实改口：** 不再写「加工路径 / 模型预览已上线」。工作经历用集成视角（`GraphicsWindowEmbedded` 嵌入 `QOpenGLWidget`，上下文 / DPI / 场景图）；项目经历写原型能力（STL、光照材质、MatrixTransform、Trackball）。并写明挂在材料编辑「关于」测试入口，**主产品 3D 仍为 OCCT/Mayo**。面试勿把 OSG 说成产线 3D。

## 投递建议（BOSS 打招呼可直接用）

您好，我做 Windows 工业上位机（C++ / Qt）。在众智激光负责切片控制软件：Qt 工艺编辑界面、OPC UA 与 PLC 读写/订阅/断线重连，并用 VS + CMake 做模块化与文档。与贵司上位机（C++/Qt/STL、PLC/OPC）方向一致，方便的话想沟通一下岗位。
